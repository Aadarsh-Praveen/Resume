[build]
builder = "NIXPACKS"
buildCommand = "pip install -r job-agent/requirements.txt"

[deploy]
startCommand = "cd job-agent && uvicorn dashboard.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
